task wrapper(type: Wrapper) {
    gradleVersion = '2.2'
}

buildscript {
    repositories {

        mavenLocal()
        mavenCentral()

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.1.0-rc1'
    }
}